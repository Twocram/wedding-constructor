---
import Ornament from './Ornament.astro';
interface Props {
  title?: string;
  eyebrow?: string;
  eyebrowGuest?: string; // шаблон с {g}: заменяет eyebrow, если в ссылке есть ?g=Имя
  variant?: 'plain' | 'surface' | 'photo';
  photo?: string;
  narrow?: boolean;
  id?: string;
}
const { title, eyebrow, eyebrowGuest, variant = 'plain', photo, narrow, id } = Astro.props;
---

<section id={id} class:list={['section', `section--${variant}`, { 'fade-up': true }]}>
  {variant === 'photo' && photo && <img class="section-bg" src={photo} alt="" loading="lazy" decoding="async" />}
  <div class:list={['wrap', { 'wrap--narrow': narrow }]}>
    {(title || eyebrow) && (
      <header class="section-head">
        {eyebrow && <p class="eyebrow" data-guest={eyebrowGuest}>{eyebrow}</p>}
        {title && <h2>{title}</h2>}
        <Ornament />
      </header>
    )}
    <slot />
  </div>
</section>
