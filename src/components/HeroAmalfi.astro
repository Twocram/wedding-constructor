---
import ScrollHint from './ScrollHint.astro';
interface Props {
  groom: string;
  bride: string;
  date: string;
  heroPhoto?: string;
}
const { groom, bride, date, heroPhoto } = Astro.props;
---

<header class="hero-am">
  <div class="stars" aria-hidden="true">
    {Array.from({ length: 18 }).map(() => <i />)}
  </div>
  <div class="hero-am-glow" aria-hidden="true"></div>

  <div class="hero-am-text">
    <p class="hero-am-date">{date}</p>
    <h1>
      <span class="hero-am-name">{groom}</span>
      <span class="hero-am-amp">&</span>
      <span class="hero-am-name">{bride}</span>
    </h1>
    <p class="hero-am-label"><span>приглашают вас</span><span>на свадьбу у моря</span></p>
  </div>

  {heroPhoto && (
    <figure class="hero-am-figure">
      <div class="hero-am-frame">
        <img src={heroPhoto} alt="" fetchpriority="high" />
      </div>
    </figure>
  )}

  <div class="hero-am-hint"><ScrollHint /></div>
</header>

<style>
  .hero-am {
    position: relative;
    min-height: 100svh;
    display: grid;
    align-content: center;
    justify-items: center;
    padding: 72px 24px 32px;
    overflow: hidden;
    isolation: isolate;
  }
  /* свечение горизонта */
  .hero-am-glow {
    position: absolute;
    inset: 0;
    z-index: -1;
    /* линейная подсветка горизонта: никаких дуг и пятен на широких экранах */
    background:
      linear-gradient(to top, rgba(210, 165, 82, 0.10), transparent 42%),
      linear-gradient(to top, rgba(80, 130, 180, 0.08), transparent 30%);
  }

  .hero-am-text { display: grid; justify-items: center; text-align: center; gap: 20px; }
  .hero-am-date {
    font-family: var(--font-label);
    letter-spacing: 0.42em;
    text-transform: uppercase;
    font-size: 0.72rem;
    color: var(--accent);
  }
  .hero-am h1 { display: grid; justify-items: center; }
  .hero-am-name {
    font-size: clamp(2.8rem, 13vw, 6rem);
    line-height: 1.05;
    letter-spacing: 0.02em;
    background: linear-gradient(180deg, #f3ecdf 0%, #ece5d8 55%, #c9bfae 100%);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }
  .hero-am-amp {
    font-family: var(--font-accent);
    font-style: italic;
    font-size: clamp(2rem, 8vw, 3.4rem);
    line-height: 1;
    margin: 0.1em 0;
    background: linear-gradient(180deg, var(--accent-2), var(--accent));
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }
  .hero-am-label {
    display: grid;
    gap: 4px;
    font-family: var(--font-label);
    text-transform: uppercase;
    letter-spacing: 0.34em;
    font-size: 0.66rem;
    color: var(--text-2);
  }

  /* портретная арка в двойной золотой рамке */
  .hero-am-figure { margin: 36px 0 0; width: min(300px, 74vw); }
  .hero-am-frame {
    position: relative;
    padding: 10px;
    border: 1px solid color-mix(in srgb, var(--accent) 60%, transparent);
    border-radius: 999px 999px 26px 26px;
    background: linear-gradient(180deg, rgba(210, 165, 82, 0.08), transparent);
  }
  .hero-am-frame::before {
    content: '';
    position: absolute;
    inset: 4px;
    border: 1px solid color-mix(in srgb, var(--accent) 28%, transparent);
    border-radius: 999px 999px 22px 22px;
    pointer-events: none;
  }
  .hero-am-frame img {
    width: 100%;
    aspect-ratio: 4 / 5;
    object-fit: cover;
    border-radius: 999px 999px 16px 16px;
    box-shadow: var(--shadow), 0 0 0 1px rgba(255, 255, 255, 0.05);
    filter: saturate(0.9) contrast(1.04);
    animation: am-zoom 24s var(--ease-out) infinite alternate;
  }
  @keyframes am-zoom { from { transform: scale(1); } to { transform: scale(1.05); } }

  .hero-am-hint { margin-top: 32px; color: var(--accent); }

  .stars { position: absolute; inset: 0; pointer-events: none; z-index: -1; }
  .stars i {
    position: absolute;
    width: 2px;
    height: 2px;
    border-radius: 50%;
    background: #fff;
    box-shadow: 0 0 6px 1px rgba(255, 255, 255, 0.5);
    animation: twinkle var(--d, 3s) ease-in-out infinite;
    opacity: 0;
  }
  .stars i:nth-child(3n) { background: var(--accent-2); box-shadow: 0 0 8px 2px rgba(232, 201, 135, 0.5); width: 3px; height: 3px; }
  .stars i:nth-child(1)  { left: 8%;  top: 12%; --d: 2.4s; }
  .stars i:nth-child(2)  { left: 20%; top: 6%;  --d: 3.1s; animation-delay: 0.6s; }
  .stars i:nth-child(3)  { left: 35%; top: 15%; --d: 2.8s; animation-delay: 1.1s; }
  .stars i:nth-child(4)  { left: 50%; top: 5%;  --d: 3.5s; animation-delay: 0.3s; }
  .stars i:nth-child(5)  { left: 64%; top: 11%; --d: 2.6s; animation-delay: 1.5s; }
  .stars i:nth-child(6)  { left: 78%; top: 7%;  --d: 3.2s; animation-delay: 0.9s; }
  .stars i:nth-child(7)  { left: 90%; top: 16%; --d: 2.9s; animation-delay: 1.8s; }
  .stars i:nth-child(8)  { left: 12%; top: 32%; --d: 3.4s; animation-delay: 0.4s; }
  .stars i:nth-child(9)  { left: 88%; top: 38%; --d: 2.7s; animation-delay: 1.3s; }
  .stars i:nth-child(10) { left: 5%;  top: 55%; --d: 3s;   animation-delay: 0.8s; }
  .stars i:nth-child(11) { left: 93%; top: 60%; --d: 3.6s; animation-delay: 1.6s; }
  .stars i:nth-child(12) { left: 25%; top: 85%; --d: 2.5s; animation-delay: 0.2s; }
  .stars i:nth-child(13) { left: 70%; top: 88%; --d: 3.3s; animation-delay: 1.4s; }
  .stars i:nth-child(14) { left: 45%; top: 92%; --d: 2.8s; animation-delay: 0.7s; }
  .stars i:nth-child(15) { left: 30%; top: 26%; --d: 3.7s; animation-delay: 2s; }
  .stars i:nth-child(16) { left: 72%; top: 24%; --d: 2.3s; animation-delay: 0.1s; }
  .stars i:nth-child(17) { left: 15%; top: 70%; --d: 3.1s; animation-delay: 1.9s; }
  .stars i:nth-child(18) { left: 82%; top: 76%; --d: 2.9s; animation-delay: 1.2s; }
  @keyframes twinkle {
    0%, 100% { opacity: 0; transform: scale(0.6); }
    50% { opacity: 0.9; transform: scale(1); }
  }

  .hero-am > :not(.stars):not(.hero-am-glow) { animation: am-rise 1.1s var(--ease-out) both; }
  .hero-am-figure { animation-delay: 0.35s !important; }
  .hero-am-hint { animation-delay: 0.7s !important; }
  @keyframes am-rise {
    from { opacity: 0; transform: translateY(24px); filter: blur(6px); }
    to { opacity: 1; transform: none; filter: none; }
  }

  @media (min-width: 900px) {
    .hero-am {
      grid-template-columns: 1fr 1fr;
      grid-template-areas: 'text figure' 'hint hint';
      align-items: center;
      column-gap: 64px;
      padding: 80px clamp(48px, 8vw, 140px) 40px;
      max-width: 1400px;
      margin: 0 auto;
    }
    .hero-am-text { grid-area: text; justify-items: start; text-align: left; }
    .hero-am h1 { justify-items: start; }
    .hero-am-name { font-size: clamp(4rem, 7vw, 7rem); }
    .hero-am-label { text-align: left; }
    .hero-am-figure { grid-area: figure; width: min(400px, 100%); justify-self: end; margin: 0; }
    .hero-am-hint { grid-area: hint; justify-self: center; margin-top: 48px; }
  }
  @media (prefers-reduced-motion: reduce) {
    .stars { display: none; }
    .hero-am > *, .hero-am-frame img { animation: none !important; }
  }
</style>
